const numeros = [1,2,3,4,5];

const quadrado = numeros.map(num => Math.pow(num,2))//.reverse();

//const quadrado = numeros.map(function (num){ return Math.pow(num,2)});

//const quadrado = numeros.map((num)=>{ return Math.pow(num,2)});

console.log(numeros);

console.log(quadrado);